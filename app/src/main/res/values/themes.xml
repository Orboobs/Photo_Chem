<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Camera" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:actionBarStyle">@style/action_bar</item>
    </style>

    <style name="Theme.Camera" parent="Base.Theme.Camera" />

    <style name="action_bar" parent="Widget.AppCompat.ActionBar">
        <item name="android:background">#00FFFFFF</item>
    </style>
</resources>